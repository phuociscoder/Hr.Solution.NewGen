import React from "react";
import { BaseListing } from "../../Common/BaseListing";

export class ConstractTable extends BaseListing{
    constructor(props)
    {
        super(props);
    }

    generateContent=() => {
        return(
            <>
            
            </>
        )
    }
}